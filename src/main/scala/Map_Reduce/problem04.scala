package Map_Reduce

object problem04 {
  def main(args: Array[String]): Unit = {
    val v = Vector(1, 2, 3, 4)
    for (i <- v) {
      println(i)
    }
    //Rewrite simpleMap.scala using for in place of map
  }
}
